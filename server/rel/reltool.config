{sys, [
       {lib_dirs, ["../..", "../ebin", "../deps/"]},
       {erts, [{mod_cond, derived}, {app_file, strip}]},
       {app_file, strip},
       {rel, "server", "1",
        [
         kernel,
         stdlib,
         sasl,
         server
        ]},
       {rel, "start_clean", "",
        [
         kernel,
         stdlib
        ]},
       {boot_rel, "server"},
       {profile, embedded},
       {incl_cond, exclude},
       {excl_archive_filters, [".*"]}, %% Do not archive built libs
       {excl_sys_filters, ["^bin/.*", "^erts.*/bin/(dialyzer|typer)",
                           "^erts.*/(doc|info|include|lib|man|src)"]},
       {excl_app_filters, ["\.gitignore"]},

       {app, server, [{incl_cond, include}]},

       {app, inets,   [{incl_cond, include}]},
       {app, sasl,   [{incl_cond, include}]},
       {app, stdlib, [{incl_cond, include}]},
       {app, public_key, [{incl_cond, include}]},
       {app, ssl, [{incl_cond, include}]},
       {app, crypto, [{incl_cond, include}]},
       {app, kernel, [{incl_cond, include}]},
       {app, mochiweb, [{incl_cond, include}]},
       {app, libprotobuf, [{incl_cond, include}]},
       {app, ejson, [{incl_cond, include}]},
       {app, erlaws, [{incl_cond, include}]},
       {app, emysql, [{incl_cond, include}]},
       {app, xmerl, [{incl_cond, include}]},
       {app, compiler, [{incl_cond, include}]},
       {app, syntax_tools, [{incl_cond, include}]},
       {app, gproc, [{incl_cond, include}]},
       {app, lager, [{incl_cond, include}]},
       {app, mnesia, [{incl_cond, include}]},
       {app, mc_user_store, [{incl_cond, include}]},
       {app, erlcloud, [{incl_cond, include}]},
       {app, jsx, [{incl_cond, include}]},
       {app, uuid, [{incl_cond, include}]}
       
      ]}.

{target_dir, "server"}.

{overlay, [
           {mkdir, "log/sasl"},
           {copy, "files/erl", "\{\{erts_vsn\}\}/bin/erl"},
           {copy, "files/nodetool", "\{\{erts_vsn\}\}/bin/nodetool"},
           {copy, "files/server", "bin/server"},
           {copy, "files/server.cmd", "bin/server.cmd"},
           {copy, "files/start_erl.cmd", "bin/start_erl.cmd"},
           {copy, "files/install_upgrade.escript", "bin/install_upgrade.escript"},
           {copy, "files/sys.config", "releases/\{\{rel_vsn\}\}/sys.config"},
           {copy, "files/vm.args", "releases/\{\{rel_vsn\}\}/vm.args"}
          ]}.
