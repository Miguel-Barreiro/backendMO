{application, server, [
    {description, ""},
    {vsn, "1"},
    {registered, []},
    {applications, [
        lager,
        kernel,
        stdlib,
        crypto,
        public_key,
        ssl,
        emysql,
        gproc,
        inets,
        mc_user_store
    ]},
    {mod, { server, []} },
    {env, [
        {port, 9000},
        {ssl_port, 9001}
    ]}
]}.
