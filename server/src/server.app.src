{application, server, [
    {description, ""},
    {vsn, "1"},
    {registered, []},
    {applications, [
        kernel,
        stdlib,
        crypto,
        public_key,
        ssl,
        emysql,
        lager,
        gproc,
        inets,
        mc_user_store
    ]},
    {mod, { server, []} },
    {env, [
        {port, 9000},
        {ssl_port, 9001}
    ]}
]}.
